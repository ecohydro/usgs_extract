# ── Download & preprocessing ──────────────────────────────────────────────────
requests
beautifulsoup4
PyPDF2
pdf2image          # poppler must be installed separately (brew install poppler)
Pillow
opencv-python
numpy<2.0
tqdm
pyyaml

# ── Table detection (Microsoft Table Transformer) ─────────────────────────────
torch
torchvision
transformers

# ── OCR / digitization ────────────────────────────────────────────────────────
# Reducto.ai is a paid API service — no Python package required
# PaddleOCR was tested but not used in final pipeline:
scikit-image
imgaug
pyclipper
lmdb
rapidfuzz
cython
albumentations==1.4.10
opencv-contrib-python
shapely

# ── Analysis notebooks (code/02_inventory/ ) ───────
pandas
geopandas
fiona              # required for GDB file reading in 04_santa_ynez.ipynb
pyarrow            # parquet read/write for processed_metadata.parquet
matplotlib
seaborn            # color palette in water_palette.py
openpyxl           # reading .xlsx files in 02_validate_accuracy.ipynb
dataretrieval      # NWIS API fallback in 03_santa_barbara and 04_santa_ynez

# ── NWIS California stream site inventory (code/03_nwis_usgs/) ───────────────
requests           # direct NWIS HTTP calls in fetch_nwis_ca_stream_sites.py
# pandas, pyarrow already listed above
jupyter
ipykernel
